git cz

// const schema = await buildSchema({
// resolvers: [__dirname + "/modules/**/*.ts"],
// authChecker: customAuthChecker
// });

// RegisterResolver,
// LoginResolver,
// MeResolver,
// ConfirmUserResolver
